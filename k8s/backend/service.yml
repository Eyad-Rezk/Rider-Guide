apiVersion: v1
kind: Service
metadata:
  namespace: rider-guide
  name: backend-svc
spec:
 type: ClusterIP
 ports:
   - port: 3000 #service port
     targetPort: 3000 #pod port
 selector:
    app: backend
